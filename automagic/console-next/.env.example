# Base URL of the Express backend API (serves /api/*). Used server-side only.
AUTOMAGIC_API_URL=http://127.0.0.1:3100

# Optional — SSE stream URL (defaults to AUTOMAGIC_API_URL). Exposed to the browser.
NEXT_PUBLIC_SSE_URL=/api/sse
