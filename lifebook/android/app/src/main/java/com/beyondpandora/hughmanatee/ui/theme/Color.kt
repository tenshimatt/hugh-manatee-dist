package com.beyondpandora.hughmanatee.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Hugh Manatee color palette.
 *
 * Ported 1:1 from app/src/lib/theme.ts (Expo).
 * Warm, calm, high-contrast — designed for elderly eyes.
 */

// Background gradient anchors
val BgTop = Color(0xFFF4ECE1)
val BgBottom = Color(0xFFE8D9C4)

// Ink — near-black, softer than pure #000
val Ink = Color(0xFF1E1B17)
val InkSoft = Color(0xFF4A443C)
val InkFaint = Color(0xFF8B8578)

// Accent — warm amber/terracotta
val Accent = Color(0xFFB8651A)
val AccentSoft = Color(0xFFE8A764)

// Surfaces
val Surface = Color(0xFFFFFFFF)
val SurfaceAlt = Color(0xFFFBF6EE)

// States
val Danger = Color(0xFF9A2A2A)
val Success = Color(0xFF2F6B3C)
val Divider = Color(0xFFD8CEBE)
