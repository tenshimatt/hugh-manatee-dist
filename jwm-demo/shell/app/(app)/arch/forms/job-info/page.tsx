import { SectionStub } from "@/components/chrome/SectionStub";
import { FileInput } from "lucide-react";
export default function P() { return <SectionStub eyebrow="Architectural · Forms" title="Job Info" description="New project intake form — short job ID, release type, description, materials, shipping target." icon={FileInput} />; }
