import { SectionStub } from "@/components/chrome/SectionStub";
import { LayoutDashboard } from "lucide-react";
export default function P() { return <SectionStub eyebrow="Architectural" title="Panel Dashboard" description="Panel release status, quantities released vs. quoted, and per-job roll-up." icon={LayoutDashboard} />; }
